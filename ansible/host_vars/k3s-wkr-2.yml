---
# Host-specific settings for worker node 2
node_labels:
  role: worker
  workload: storage
  environment: production

# Optional: Node-specific taints (if needed)
# node_taints:
#   - "dedicated=storage:NoSchedule"

# Optional: Worker-specific resource configurations
system_settings:
  vm.max_map_count: 262144
  fs.file-max: 65536

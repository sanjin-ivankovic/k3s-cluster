[master]
${primary.name} ansible_host=${primary.ip} ansible_user=sanjin

[workers]
%{ for node in workers ~}
${node.name} ansible_host=${node.ip} ansible_user=sanjin
%{ endfor }

[k3s_cluster:children]
master
workers
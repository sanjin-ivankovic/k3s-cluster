# Ansible inventory for K3s cluster: ${cluster_name}
# Generated by Terraform

[master]
%{ for node in master_nodes ~}
${node.ip} ansible_user=${ssh_user} node_role=master
%{ endfor ~}

[node]
%{ for node in worker_nodes ~}
${node.ip} ansible_user=${ssh_user} node_role=worker
%{ endfor ~}

[k3s_cluster:children]
master
node
